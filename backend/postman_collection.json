{
  "info": {
    "name": "ecomarket-backend",
    "_postman_id": "ecomarket-backend-collection",
    "description": "Colección para probar la API backend de ecomarket (register, login, upload, categories, products)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Auth - Register",
      "event": [
        {
          "listen": "prerequest",
          "script": { "exec": ["pm.environment.set('timestamp', Date.now());"] }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"username\": \"juan_{{timestamp}}\",\n  \"email\": \"juan_{{timestamp}}@example.com\",\n  \"password\": \"MiPass123\"\n}"
        },
        "url": {
          "raw": "{{baseUrl}}/auth/register",
          "host": ["{{baseUrl}}"],
          "path": ["auth","register"]
        }
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "var json = pm.response.json();",
              "if (json && json.token) { pm.environment.set('jwt', json.token); }",
              "if (json && json.token) { console.log('Saved jwt from register'); }"
            ]
          }
        }
      ]
    },
    {
      "name": "Auth - Login",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"usernameOrEmail\": \"juan\",\n  \"password\": \"MiPass123\"\n}"
        },
        "url": {
          "raw": "{{baseUrl}}/auth/login",
          "host": ["{{baseUrl}}"],
          "path": ["auth","login"]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "var json = pm.response.json();",
              "if (json && json.token) { pm.environment.set('jwt', json.token); }",
              "if (json && json.token) { console.log('Saved jwt from login'); }"
            ]
          }
        }
      ]
    },
    {
      "name": "Images - Upload",
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "formdata",
          "formdata": [
            {
              "key": "file",
              "type": "file",
              "src": "{{uploadFilePath}}"
            }
          ]
        },
        "url": {
          "raw": "{{baseUrl}}/images/upload",
          "host": ["{{baseUrl}}"],
          "path": ["images","upload"]
        }
      },
            "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "try { var json = pm.response.json(); if (json && json.filename) { pm.environment.set('lastImage', json.filename); pm.environment.set('lastImageUrl', json.url); } } catch(e) { console.log('Response not JSON or empty'); }",
              "console.log('Saved lastImage:', pm.environment.get('lastImage'))"
            ]
          }
        }
      ]
    },
    {
      "name": "Categories - Create",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" },
          { "key": "Authorization", "value": "Bearer {{jwt}}" }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"name\": \"Electrónica\",\n  \"description\": \"Gadgets y dispositivos\"\n}"
        },
        "url": {
          "raw": "{{baseUrl}}/categories",
          "host": ["{{baseUrl}}"],
          "path": ["categories"]
        }
      }
    },
    {
      "name": "Products - Create",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" },
          { "key": "Authorization", "value": "Bearer {{jwt}}" }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"name\": \"Cámara X100\",\n  \"description\": \"Cámara compacta 20MP\",\n  \"price\": 199.99,\n  \"categoryId\": 1,\n  \"stock\": 10,\n  \"imageFilename\": \"{{lastImage}}\"\n}"
        },
        "url": {
          "raw": "{{baseUrl}}/products",
          "host": ["{{baseUrl}}"],
          "path": ["products"]
        }
      }
    },
    {
      "name": "Products - List",
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/products?page=0&size=10",
          "host": ["{{baseUrl}}"],
          "path": ["products"],
          "query": [
            { "key": "page", "value": "0" },
            { "key": "size", "value": "10" }
          ]
        }
      }
    },
    {
      "name": "Products - Get by ID",
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt}}" }
        ],
        "url": {
          "raw": "{{baseUrl}}/products/{{productId}}",
          "host": ["{{baseUrl}}"],
          "path": ["products","{{productId}}"]
        }
      }
    },
    {
      "name": "Images - Serve",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/images/{{lastImage}}",
          "host": ["{{baseUrl}}"],
          "path": ["images","{{lastImage}}"]
        }
      }
    }
  ]
}
